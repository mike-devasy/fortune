{
  "name": "d-cuatrobet-com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:server": "npm run build && node ../../template/scripts/build-server.mjs --server-dir ../../server"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
